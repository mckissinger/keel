# measure — load-bearing seams (file-path<TAB>literal-anchor).
# One file per feature; never edit another feature's anchor file (the §4 rule).
# The doctrine's cohort-attribution rule — the measurement grain-line, pinned
# VERBATIM (the source sentence sits on one physical line).
references/growth-operations.md	**Attribution is cohort-level only: the readout joins campaigns to product outcomes at campaign/channel granularity via tagged links, and never links an outreach contact's identity to a product account.**

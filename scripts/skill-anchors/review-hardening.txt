# review-hardening — load-bearing seams (file-path<TAB>literal-anchor).
# One file per feature; never edit another feature's anchor file (the §4 rule).
# Positive anchors: the new load-bearing sentences.
skills/review-feature/SKILL.md	The composition is the intent; the screenshot is the reality; the gap is the finding.
skills/provision/SKILL.md	Seed `specs/run-command-inventory.txt` alongside this allowlist
skills/debug/SKILL.md	debug owns diagnosis, not landing
# Negative anchors ('!' prefix): the committed form of the workbench convergence —
# no converged file may again require design/mockups/ as a precondition or call a
# mockup "the intent". Scoped to the three converged files.
!skills/review-feature/SKILL.md	The mockup is the intent
!skills/implement-milestone/SKILL.md	The feature's mockups (`design/mockups/<feature>/`) and `specs/design.md` exist
!skills/spec-feature/references/no-foundation.md	the mockups (`design/mockups/<feature>/`)

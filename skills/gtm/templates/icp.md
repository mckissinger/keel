# ICP — <product>

<!-- Sharpened from the positioning canvas's "who cares" section. Lands as
     specs/gtm/icp.md in the plan PR. Narrow enough to write ONE message to —
     an ICP that needs two messages is two ICPs; pick the primary. -->

## Audience slice

<!-- The segment, made concrete: role/title, company or context shape, size
     band, geography if it matters, and the situation that makes them in-market
     (the trigger, not just the demographic). -->

## Qualification signals

<!-- The observable marks that separate a fit from a lookalike — tools they
     already use, behaviors, public footprints, firmographic lines. Each signal
     should be checkable from outside (a website, a listing, a profile), because
     these become a campaign's list-building filters. -->

## Where they live / reachability

<!-- Per candidate channel: where this slice actually spends attention, whether
     they can be reached there at tolerable volume, and how warm the medium is
     for them. These notes feed the channel scorecard's fit column. -->

## Data-coverage caveat

<!-- Mandatory. Reachability above is a HYPOTHESIS until spec-campaign
     sample-validates an enrichment source against this exact slice — data
     providers' coverage is audience-dependent and never assumed (as of 2026-07,
     niche trades and non-US segments are routinely thin in mainstream
     databases). Record here which claims are validated and which await a
     sample. -->

## Founder-must-do

<!-- What only the founder can supply for this ICP — warm intros, community
     standing, domain credibility. Tag each [attended] in any conditions the
     plan authors. -->

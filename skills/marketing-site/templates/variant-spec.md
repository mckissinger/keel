# Variant — <name>

<!-- One per Explore-round variant (2–3 per round). The amplification-round unit:
     each variant is the SAME committed design language at marketing spectacle —
     the axes below are what differ. Throwaway: the mockup under
     design/marketing/variants/ picks a direction and is never re-implemented as
     the spec. -->

## Hero treatment

<!-- One from the menu (references/hero-treatments.md): staged product shots /
     generated image / CSS gradient-motion / WebGL scene / typographic.
     One sentence on the specific move — what carries the first screen. -->

## Section selection

<!-- The pruned ladder for this variant (references/conversion-craft.md) — which
     sections, in what order, and the one job each does. Variants may prune
     differently; the conversion goal stays fixed. -->

## Tone

<!-- The copy voice at spectacle: e.g. confident-terse, warm-explanatory,
     technical-direct. Named so the mockup's copy and the eventual page agree. -->

## Amplification notes

<!-- What gets amplified from the committed design language and how far: type scale
     jump, palette intensity, motion posture. What must NOT drift (the token source,
     the brand constraints from the brief). -->

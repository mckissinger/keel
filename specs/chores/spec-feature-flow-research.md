# Chore batch — spec-feature-flow-research

Punch-list batch: add the scoped reference-research paragraph to spec-feature's Movement 1 interview — flow-level pattern research (Mobbin `search_flows`) is allowed during state/interaction enumeration, landing only as UX-completeness done-conditions, with looks-level reference pulls staying gated at Movement 2's novel-archetype divergence-sketch case.

## Applied items

- **flow-research-line** — `skills/spec-feature/SKILL.md` Movement 1: a "Scoped reference research — flows, not looks." paragraph now sits between the interview's enumeration bullets and the one-paragraph-definition close, permitting a connected design-reference MCP's flow-level search for interaction patterns and directing findings into UX-completeness done-conditions, never tokens or visual choices.
  - Done-condition: the paragraph appears after the Cross-feature seams bullet and before the "End the interview" close; it names Mobbin's `search_flows` as the known instance and states that reference pulls for looks stay gated at Movement 2's novel-archetype divergence-sketch case.

## Combined checks

Self-tests `bash scripts/check-verified-pin.test.sh`, `check-neutral.test.sh`, `session-bootstrap.test.sh`, `merge-guard.test.sh`, `guard-branch-rules.test.sh`, `attended-marker-parity.test.sh`, `check-auto-preflight.test.sh`, `check-plan.test.sh`, `check-skill-frontmatter.test.sh`, `check-skill-anchors.test.sh` — all PASS; repo lints `check-neutral.sh`, `check-plan.sh`, `check-skill-frontmatter.sh`, `check-skill-anchors.sh` — all PASS; `claude plugin validate --strict .` — Validation passed. All green on the combined branch.

verified: clean at 1a75843, 2026-07-10, via punch-list (evidence in PR #98)

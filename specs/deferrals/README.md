# Deferrals

File-per-entry ledger of improvements deliberately parked — each gated on something that must
be resolved first. One `<slug>.md` per open item; move closed items into `_closed.md` with the
resolution.

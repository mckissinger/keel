# Skill trigger evals + budget lint

**Parked 2026-07-01.** Committed should-trigger / should-not-trigger prompt sets for the
confusable grain boundaries (spec-foundation/spec-feature/spec-change; kickoff/adopt/
interview; implement-feature/implement-milestone; punch-list vs spec-change), run in fresh
sessions keel-enabled vs disabled; plus a frontmatter lint (grain named, triggers present,
NOT-this scoping). Descriptions were restructured in the packaging chore without measurement
— un-pinned claims by keel's own philosophy.

**Gate:** L-sized; needs an eval-runner shape decision (skill-creator's evals.json format vs
a bespoke harness) and a re-run trigger policy (description changes, new models). Revisit
after the Wave 2/3 mechanics land and descriptions have settled.

# Uncertain choice — the subsection sits immediately AFTER the numbered stacked-series sequence, at `##`

**Choice made.** The wave-scripting contract is inserted between `## A stacked series (m1 ← m2 ← m3)`
and `## When a review fix forces a rebase (the cascade)` — `skills/land-feature/SKILL.md:29-35` — as a
sibling `##` section, matching the file's flat heading structure (it has no `###` level anywhere).

**Viable alternative considered.** Place it after `## Reconcile the specs to merged reality`, i.e. as
the last wave section immediately preceding `## Boundaries`; or nest it as a `###` inside the
stacked-series section.

**Why it's uncertain.** The done-condition says only "placed with the stacked-series material, before
Boundaries," which both positions satisfy — every section between the numbered sequence and Boundaries
is stacked-series material. The tie was broken on what the contract references: its deletion-scope rule
is the scripted form of the numbered sequence's **step 2**, and the bullet cites "step 2" by name, so
adjacency keeps that citation local and readable. Introducing a `###` level was rejected to keep the
existing structure's diff empty in spirit as well as in content. A reviewer who reads the contract as a
wave-wide epilogue (it also constrains the check-wait that gates *every* merge in the wave, including
the consolidated check) would put it last, just above Boundaries.

**No behavioral difference either way** — the placement changes reading order only; both rules, the
scar, and the anchors are identical in either position.
